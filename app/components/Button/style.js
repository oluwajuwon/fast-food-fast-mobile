import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btn: {
    borderRadius: 5,
    maxWidth: '50%',
    maxHeight: '40%',
    padding: 10,
  },
  btnText: {
    color: 'white'
  }
});

export default styles;